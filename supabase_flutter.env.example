# 1) cp supabase_flutter.env.example supabase_flutter.env
# 2) 编辑 supabase_flutter.env：把下面两行换成 Dashboard 里的真实值（勿提交该文件，已 gitignore）
#    Supabase → Project Settings → API → Project URL、anon public key
#
# 禁止保留占位符 your_project_ref / example.com，否则注册请求会打到假域名并报 Load failed。
#
# 产品策略：未登录可「只逛逛」；保存记录需邮箱/Google。可在 Dashboard 关闭 Anonymous。
#
# Google 一键登录（可选）：
#   1) Google Cloud Console → OAuth 2.0 Client（Web）→ 授权重定向 URI 填
#      https://<project-ref>.supabase.co/auth/v1/callback
#   2) Supabase → Authentication → Providers → Google：打开开关，填入 Client ID / Secret
#   3) Authentication → URL Configuration：Redirect URLs 加入你的前端地址（生产站必加），例如
#      https://hi-doo.vercel.app/**   http://localhost:3000/**   http://127.0.0.1:3000/**
#      （Site URL 可设生产域名，避免 OAuth 默认跳回 localhost）

SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.replace-with-your-anon-key
