# Git / IDE
.git
.gitignore
.cursor

# Flutter（镜像内只用已编译的 build/web）
.dart_tool
.flutter-plugins-dependencies
.packages
**/doc/api/

# 不在镜像里安装本机/开发依赖
api/node_modules
node_modules

# 原生工程（Web 部署不需要）
android/
ios/
linux/
macos/
windows/
test/

# 本地密钥与上传（运行时挂卷或注入环境变量）
api/.env
api/uploads/
api/certs/

# Flutter build 目录：只保留 build/web 进镜像
build/*
!build/web
!build/web/**
