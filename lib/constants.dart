/// App-wide defaults. Build-time `--dart-define=DONATION_URL=...` overrides [defaultDonationUrl].
class AppConstants {
  AppConstants._();

  static const String defaultDonationUrl = 'https://buymeacoffee.com/sharonwangl';
}
