/// Desktop/mobile VM targets: product ships Web only; transcription uses API Whisper (same as stub).
bool get canUseLocal => false;

Future<String?> transcribeWithLocal(String audioPath) async => null;
